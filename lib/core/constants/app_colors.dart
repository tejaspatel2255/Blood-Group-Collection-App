import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF006D5B); // Deep Teal
  static const Color secondary = Color(0xFFF5A623); // Golden Amber
  static const Color background = Color(0xFFF8F9FA); // Soft White
  static const Color cardBackground = Color(0xFFFFFFFF); // Pure White
  static const Color error = Color(0xFFE53935);
  static const Color success = Color(0xFF43A047);

  // Additional useful colors
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);
  static const Color divider = Color(0xFFBDBDBD);
  static const Color inputBackground = Color(0xFFEEEEEE);
}
